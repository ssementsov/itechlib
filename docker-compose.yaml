version: '3.5'
services:
  nginx:
    build:   
      context: ./nginx
      dockerfile: Dockerfile
    restart: always  
    ports:
      - 90:90
    networks:
      - lib
    container_name: nginx_lib
    volumes:
      - front:/usr/share/nginx/html
networks:
  lib:
    name: lib 
volumes:
  front:
    name: 
