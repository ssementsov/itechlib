pipeline {
    agent {
        dockerfile { filename './React/Dockerfile2' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
