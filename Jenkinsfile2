pipeline {
          agent any
           options {
		timestamps ()
	    } 
	
	stages{
		   stage('Start NodeJS'){
                    			steps{
				                     sh '''
		 			              	   docker-compose2.yaml up -d ''' 
			    	               }
      }
  }
}
		 
		   
