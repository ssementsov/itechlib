pipeline {
          agent any
           options {
		timestamps ()
	    } 
	
	stages{
               stage('Create docker'){
			agent{
			    dockerfile{ filename './React/Dockerfile'
	                        args '--name node -v exchange:/exchange  '
					  	
				    }
	            }
		   stage('Start NodeJS'){
                    			steps{
				                     sh '''
		 			              	   docker-compose up -d ''' 
			    	               }
      }
  }
}
		 
		   
