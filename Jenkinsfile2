pipeline {
          agent any
           options {
		timestamps ()
	    } 
	
	stages{
		   stage('Start NodeJS'){
                    			steps{
				                     sh '''
		 			              	   docker-compose up -d ''' 
			    	               }
      }
  }
}
		 
		   
